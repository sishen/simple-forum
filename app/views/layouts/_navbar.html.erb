<div class="navbar">
  <div class="navbar-inner">
    <a class="brand" href="#">Simple Forum</a>
    <ul class="nav pull-right">
      <% if logged_in? %>
        <li><a href="#"><%= current_user.name %></a></li>
        <li>
          <a href="/logout">Logout</a>
        </li>
      <% else %>
        <li><a id="signup-link" href="#signup" data-toggle="modal">Sign Up</a></li>
        <li><a id="login-link" href="#login" data-toggle="modal">Log In</a></li>
      <% end %>
    </ul>
  </div>
</div>
